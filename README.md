# Worldbuilding of Spiral Knights

This repository contains the sources of a study of Spiral Knights fictional setting, its lore, mythology, and worldbuilding ([`src/lib/burning_stars.mdx`](./src/lib/burning_stars.md)).

The document can be built for multiple mediums: `.pdf` or a webpage.

Build requirements:

- `Node` or other compatible javascript runtime
- `npm`-compatible package manager

1. Install the dependencies:

  ```sh
  npm install
  ```

2. Refer to [taskfile](./taskfile.yml) for further available actions

For pdf generation (debian-only) install additional packages:

```sh
apt install weasyprint wkhtmltopdf
```

## Credits

For credit, please see [CREDITS.md](./CREDITS.md).
